@model IEnumerable<Proyect.Models.Desarrollador>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Admin)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Admin)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DesarrolladorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DesarrolladorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DesarrolladorID })
        </td>
    </tr>
}

</table>
